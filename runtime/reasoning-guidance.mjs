export const REASONING_GUIDANCE = 'Treat the current request as a precise specification. Distinguish objects, their identities, their changing properties, and the requested result. Preserve the meaning and scope of references and quantifiers. Determine which actions or choices are permitted and which facts remain unknown before deriving a conclusion or optimizing a worst case. Do not discard a stated condition because a familiar template does not use it. Check that the result satisfies every constraint; if an assumption is needed, make it explicit. Respect the requested language and output format without changing the facts or reasoning.';
